#파이썬
a,b = map(int , input().split())
A = list(map(int, input().split()))    #list 로 사용 
for i in A:                         #range를 사용하는것이 아닌 그냥 in A 를 바로 사용 
    if i<b:
        print(i, end = ' ')   #줄이넘어가기에 end = ' ' 를사용
        
        
//c++
#include<iostream>

using namespace std;

int main() {
	int a; int b;
	cin >> a >> b;
	int c;
	for (int i = 0; i < a; i++) {
		cin >> c;
		if (c < b) {
			cout << c << " ";
		}
	}

}

//i만큼 반복되는 것이기에 입력받을 수 있게 된다. 
